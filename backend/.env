# Django Settings
SECRET_KEY=django-insecure-_0w0pd2mon&drk%ifnfn)+qt-$1ox&ag-!rk*c*w5*4l&dndo8
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings (MySQL)
DB_ENGINE=django.db.backends.mysql
DB_NAME=electric_store
DB_USER=Rith
DB_PASSWORD=123456
DB_HOST=localhost
DB_PORT=3307

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Email Settings (for OTP - currently using console backend for development)

# ------------------------------------------------------------------
# Real email / SMS settings (examples)
# To send real emails, replace the console backend above with the SMTP
# backend and provide credentials. Do NOT commit real credentials.
# Example (Gmail SMTP):
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password  # use App Password if using Google
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# Example (SendGrid via SMTP):
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=your-sendgrid-api-key
# DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# Optional: Anymail provider settings (uncomment to use):
# ANYMAIL_PROVIDER=sendgrid
# ANYMAIL_API_KEY=your-anymail-or-sendgrid-key

# Twilio SMS settings (optional). Fill these to enable SMS OTP sending.
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_PHONE_NUMBER=+1234567890

# Notes:
# - For local testing leaving EMAIL_BACKEND as the console backend will
#   print OTPs to the server console. To send real email, set the SMTP
#   backend and restart the Django server.
# - Never store real secrets in VCS; keep them in this `.env` which
#   should be excluded from source control, or use a secrets manager.
# ------------------------------------------------------------------

